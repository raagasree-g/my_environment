[project]
name = "my_env"
version = "0.1.0"
description = "Customer Support RL Environment"
requires-python = ">=3.10"
dependencies = [
    "openenv-core>=0.2.0",
]

[project.scripts]
server = "server.app:main"

[tool.openenv]
entrypoint = "env.py"